# API-CRUD
---
Your task is to create seed data for a json api using MongoDB and Mongoose.

This repo serves as a model for establishing a seed and running it to connect with MongoDB, generating schemas and including a schema within a schema.